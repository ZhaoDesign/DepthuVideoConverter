# ------------------------------------------------------------------
# Depth Video Converter — Python dependencies
#
# Install:  pip install -r requirements.txt
# ------------------------------------------------------------------

# --- Legacy Web UI compatibility (not used by the native desktop entry) ---
gradio>=4.0,<6.0

# --- Deep-learning runtime ---
torch>=2.1
torchvision>=0.16

# --- Image / video processing ---
opencv-python>=4.8
numpy>=1.24

# --- Historical REST API compatibility (not used by the native desktop entry) ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# --- Native desktop UI ---
PySide6>=6.7

# --- Not required but recommended ---
# accelerate>=0.25   (optional — speeds model loading on multi-GPU setups)
